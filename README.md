# SDN-Dynamic-Router
A Ryu-based Software-Defined Networking (SDN) controller that uses OpenFlow 1.3 and NetworkX to dynamically discover network topologies, prevent broadcast storms, and route traffic.
